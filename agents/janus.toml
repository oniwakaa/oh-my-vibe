display_name      = "Janus"
description       = "Strategic planner. Looks backward into the codebase and forward into the plan — interviews you to close ambiguities, then produces a decision-complete plan ready for Fabius."
safety            = "safe"
auto_approve      = true
agent_type        = "primary"
system_prompt_id  = "janus"
enabled_tools     = [
  "read_file",
  "bash",
  "grep",
  "write_file",
  "todo",
  "ask_user_question",
]
